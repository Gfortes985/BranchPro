{
  "name": "BranchPro",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev:editor": "pnpm -C apps/editor-desktop dev",
    "install:editor": "pnpm -C apps/editor-desktop install",
    "approve": "pnpm approve-builds",
    "dist": "npm build && electron-builder"
  },
  "devDependencies": {
    "turbo": "^2.8.3"
  },
  "dependencies": {
    "electron": "^40.2.1"
  }
}
